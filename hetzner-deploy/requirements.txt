# ============================================================
# TradeMatrix.ai - Hetzner Deployment Requirements
# ============================================================
# Python 3.11+ required
# Updated: 2025-11-02 - COMPLETE dependency list
# ============================================================

# ============================================================
# CORE DEPENDENCIES
# ============================================================

# Task Queue & Scheduling
celery==5.4.0                    # Background task processing
redis==5.2.0                     # Message broker & cache

# Database & Backend
supabase==2.9.0                  # Supabase Python client
python-dotenv==1.0.1             # Environment variable management

# ============================================================
# DATA FETCHING & APIs
# ============================================================

# Market Data APIs
finnhub-python==2.4.20           # Stock/index prices (DAX, NDX, DJI)
requests>=2.31.0                 # HTTP library (Alpha Vantage, general API calls)
aiohttp>=3.9.0                   # Async HTTP for concurrent EOD data fetching
httpx==0.27.0                    # Modern HTTP client (chart images, async support)

# Data Processing
pyyaml>=6.0.1                    # YAML config file parsing (eod_data_fetcher.py)
numpy>=1.24.0                    # Numerical computations (signal_bot.py)

# ============================================================
# PUSH NOTIFICATIONS
# ============================================================

pywebpush==2.0.1                 # Web Push Protocol (browser notifications)
cryptography==44.0.0             # Required by pywebpush (VAPID keys)

# ============================================================
# AI AGENTS & REPORT GENERATION
# ============================================================

# OpenAI (ChartWatcher, JournalBot)
openai>=1.0.0                    # OpenAI API client (GPT-4, Vision API)

# Report Generation (JournalBot)
python-docx>=1.1.0               # DOCX report generation
reportlab>=4.0.0                 # PDF report generation
pillow>=10.0.0                   # Image processing (charts in reports)

# ============================================================
# UTILITIES & SUPPORT
# ============================================================

# Data Validation & Models
pydantic>=2.5.0                  # Data validation (FastAPI integration ready)

# Timezone & Date Handling
pytz>=2024.1                     # Timezone handling (MorningPlanner, USOpenPlanner)

# ============================================================
# OPTIONAL (for development/testing)
# ============================================================

# FastAPI (for future chart API endpoints - currently not active)
# fastapi>=0.110.0               # REST API framework
# uvicorn[standard]>=0.27.0      # ASGI server for FastAPI

# ============================================================
# DEPLOYMENT NOTES
# ============================================================
# To install on Hetzner server:
#   pip install -r requirements.txt
#
# Docker Compose services:
#   - redis:7-alpine (message broker)
#   - celery worker (background tasks)
#   - celery beat (scheduler)
#
# Environment variables required (see .env.example):
#   - SUPABASE_URL, SUPABASE_SERVICE_KEY
#   - FINNHUB_API_KEY, ALPHA_VANTAGE_API_KEY
#   - OPENAI_API_KEY
#   - VAPID_PUBLIC_KEY, VAPID_PRIVATE_KEY, VAPID_EMAIL
#   - CELERY_BROKER_URL, CELERY_RESULT_BACKEND
# ============================================================
